#include "../include/DisplayCode.hpp"

DisplayCode::DisplayCode(int n, int size, int x, int y):
    mNumberOfLine(n),
    mSizeOfText(size){
    // mBound(x, y),
    // mTextChild(),
    // mRecChild(){
        // sf::Vector2u winBound(1400, 1000);
        // std::cout << winBound.x << " " << winBound.y << "\n";
        // for(int i = 0; i < n; ++i){
        //     sf::RectangleShape rec(mBound);
        //     rec.setFillColor(sf::Color::Blue);
        //     rec.setPosition(winBound.x - mBound.x, winBound.y - (mNumberOfLine - i) * mBound.y);
        //     mRecChild.push_back(rec);
        // }
}

// void DisplayCode::draw(sf::RenderTarget &target){
    // for(auto child : mRecChild)
        // target.draw(child);
    // target.draw(mText, states);
// }



